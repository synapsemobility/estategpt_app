// // urlOpener.ts
// import { Linking } from 'react-native';
// import { UrlOpenerOptions } from 'aws-amplify';

// export async function urlOpener(url: string, redirectUrl: string): Promise<UrlOpenerOptions> {
//   await Linking.openURL(url);
//   return { type: 'success', url: redirectUrl };
// }
